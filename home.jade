doctype
html
head
	meta(charset='utf-8')
	style.
		body {
			font-family: sans-serif;
		}
		.node {
			stroke-width: 1.5px;
		}
		.grup:not(:hover) .text {
			display: none;
		}
body
	h1= 'Twit @' + name
	h3!= 'True Friends: ' + common.length + '&emsp;Following Only: ' + following.length + '&emsp;Followers Only: ' + followers.length

	script(src='d3.v3.min.js')
	script!= 'window.twitinject = {following: ' + JSON.stringify(following) + ', followers: ' + JSON.stringify(followers) + ', common: ' + JSON.stringify(common) + '};'
	script(src='script.js')
